# Challenge 3: Mapping

## 🎯 Goal
Store data per user using mappings.

## 🛠️ Task
Create a function `deposit` that:
- takes an amount
- stores it in balances using msg.sender

## 💡 Hint
Use: balances[msg.sender]
