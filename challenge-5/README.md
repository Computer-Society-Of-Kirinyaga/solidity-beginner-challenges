# Challenge 5: Access Control

## 🎯 Goal
Restrict function access.

## 🛠️ Task
Create a function `setValue` that:
- only the owner can call
- updates the value variable

## 💡 Hint
Use: require(msg.sender == owner)
